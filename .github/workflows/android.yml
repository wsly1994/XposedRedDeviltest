name: Android CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    # 🔥 核心修复步骤：使用 sed 命令强行删除 build.gradle 里所有关于签名的配置
    # 删掉后，Gradle 就会找不到自定义签名，从而自动降级使用通用的测试签名
    - name: Remove Broken Signing Config
      run: |
        sed -i '/signingConfig/d' app/build.gradle
        sed -i '/signingConfigs/d' app/build.gradle
        sed -i '/storeFile/d' app/build.gradle

    - name: Build with Gradle (Debug Mode)
      run: ./gradlew assembleDebug -x lint

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/*.apk
